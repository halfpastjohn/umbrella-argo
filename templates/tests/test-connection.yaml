apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cylance-service.fullname" . }}-test-connection"
  labels:
    {{- include "cylance-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cylance-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
