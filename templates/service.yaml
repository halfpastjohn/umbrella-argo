apiVersion: v1
kind: Service
metadata:
  name: {{ include "cylance-service.fullname" . }}
  labels:
    {{- include "cylance-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cylance-service.selectorLabels" . | nindent 4 }}
